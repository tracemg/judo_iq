# JudoIQ Core

`core` przechowuje kontrakty danych pomiedzy analiza w `backend` a aplikacja webowa w `front-end`.

Ten katalog nie powinien zawierac logiki YOLO/OpenCV ani kodu UI. Jego rola to stabilny format wymiany danych: schematy JSON, opisy API i modele transportowe.
